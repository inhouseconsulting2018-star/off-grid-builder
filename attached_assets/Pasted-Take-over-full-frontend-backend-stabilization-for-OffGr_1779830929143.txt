Take over full frontend/backend stabilization for OffGrid Solar Builder and make sure the app is running properly end-to-end.

Main goal:
The app must work as a paid solar report product. The paywall must accept Stripe payments in test mode first, unlock the correct report after payment, and block unpaid users from paid report data.

Do not add unnecessary new features.
Do not expose secrets.
Do not hardcode Stripe/API keys.
Do not switch to live Stripe until test mode works.
Do not move or create launch tags yet.
Do not weaken project privacy or entitlement security.

PART 1 — Confirm current state
1. Confirm current branch.
2. Confirm latest launch branch is up to date.
3. Confirm working tree status.
4. Confirm no .env files are committed.
5. Confirm no Stripe, NREL, Google, OpenAI, Anthropic, or ADMIN secrets are committed.
6. Confirm required env vars are documented.

Required env vars:
- DATABASE_URL
- ADMIN_TOKEN
- STRIPE_SECRET_KEY or Replit Stripe connector equivalent
- STRIPE_WEBHOOK_SECRET
- STRIPE_HOMEOWNER_REPORT_PRICE_ID
- STRIPE_PROPERTY_PACK_PRICE_ID
- STRIPE_CONTRACTOR_ANNUAL_PRICE_ID
- STRIPE_CONTRACTOR_LIFETIME_PRICE_ID
- STRIPE_PRICE_ID fallback
- NREL_API_KEY

PART 2 — Frontend QA
Check every major frontend route and fix bugs:
- landing page
- pricing page
- project wizard
- project dashboard
- project detail page
- results/free preview page
- locked report sections
- checkout/unlock buttons
- payment success page
- payment cancel page
- full report page
- PDF download button
- admin/settings page
- admin/purchases page
- mobile layout

Frontend requirements:
1. Free preview must show only ranges.
2. Free preview must NOT show exact paid details.
3. Locked cards must clearly show what is unlocked after payment.
4. Unlock buttons must create the correct Stripe checkout session.
5. Pricing displayed must be:
   - Homeowner Full Report: $19
   - Property Pack: $39
   - Contractor Annual: $199/year
   - Contractor Lifetime Beta: $299 one-time
6. Frontend must not expose Stripe secret keys.
7. Frontend must not generate full paid report/BOM client-side before payment.
8. Frontend must handle payment success and refresh entitlement.
9. Frontend must show friendly errors if checkout fails.
10. Frontend must work on mobile.

PART 3 — Backend/API QA
Check every backend/API route and fix bugs:
- project create
- project read
- project update
- project delete
- calculate preview
- paid report API
- PDF API
- Stripe checkout session create
- Stripe webhook
- admin settings
- admin purchases
- geocoding
- PVWatts/NREL service

Backend requirements:
1. No public project enumeration.
2. No project-by-ID access without accessToken or future owner auth.
3. Update/delete/calculate/report/PDF routes require accessToken.
4. Free preview endpoint only returns preview/range data.
5. Full report endpoint verifies paid entitlement before returning paid data.
6. PDF endpoint verifies paid entitlement before generating/downloading PDF.
7. Full BOM/equipment list must be server-side only and protected.
8. Admin endpoints must require ADMIN_TOKEN bearer auth.
9. Stripe webhook must verify signature using STRIPE_WEBHOOK_SECRET.
10. Webhook must update entitlement/payment fields correctly.

PART 4 — Stripe Checkout/payment fix
Make Stripe test mode work end-to-end.

Stripe requirements:
1. Create or verify checkout endpoint:
   POST /api/stripe/create-checkout-session
2. Checkout request must include:
   - projectId
   - accessToken
   - selectedPlan
3. Checkout session metadata must include:
   - projectId
   - accessToken
   - selectedPlan
   - creditAmount
4. Correct Stripe price IDs must be used:
   - homeowner_report uses STRIPE_HOMEOWNER_REPORT_PRICE_ID or STRIPE_PRICE_ID fallback
   - property_pack uses STRIPE_PROPERTY_PACK_PRICE_ID
   - contractor_annual uses STRIPE_CONTRACTOR_ANNUAL_PRICE_ID
   - contractor_lifetime_beta uses STRIPE_CONTRACTOR_LIFETIME_PRICE_ID
5. Success URL and cancel URL must use production domain when deployed:
   https://offgridsolarbuilders.com
6. In local/dev mode, success/cancel URLs may use local/Replit preview URL.
7. Checkout must open without errors.
8. Payment must complete in Stripe test mode.
9. Webhook must receive checkout.session.completed.
10. Webhook must mark payment as complete.
11. Webhook must unlock the correct entitlement.

Plan entitlement rules:
- homeowner_report unlocks 1 full report for the current project
- property_pack adds 3 full report credits
- contractor_annual adds contractor access and 50 report credits
- contractor_lifetime_beta adds contractor access and 100 report credits

PART 5 — Paywall enforcement
Test and fix paywall logic.

Before payment:
- full report API must return 402/403 or locked response
- PDF endpoint must return 402/403 or locked response
- full BOM must not be returned
- exact monthly production must not be returned if paid-only
- hidden printable report DOM must not exist
- user only sees free preview ranges

After payment:
- full report API returns full report
- PDF endpoint returns downloadable PDF
- BOM/equipment list is visible
- monthly production is visible
- losses breakdown is visible
- entitlement persists after refresh

PART 6 — Database/migration check
Confirm database supports:
- project accessToken
- ownerUserId
- isGuestProject
- entitlementType
- reportCredits
- creditsUsed
- selectedPlan
- paidAmount
- paidAt
- stripeSessionId
- stripePriceId
- paymentStatus

If schema/migration is missing:
1. Create the migration.
2. Add migration instructions.
3. Do not pretend migration was applied if it was not.
4. Ensure existing projects get access tokens safely.

PART 7 — Geocoding/location check
Fix location/map issues.

Known test address:
2365 Myers Dr, Santa Rosa, CA 95403

Requirements:
1. Always geocode full street address first.
2. Store lat/lng when exact address is resolved.
3. Use saved lat/lng first.
4. Never overwrite exact coordinates with city-center fallback.
5. Show locationAccuracy:
   - exact_address
   - approximate_zip
   - approximate_city
   - manual_coordinates
   - failed
6. Add manual latitude/longitude override if missing.
7. Add Re-geocode Address button if missing.
8. If exact geocode fails, clearly label result approximate.

PART 8 — Build and automated checks
Run and fix until passing:
- TypeScript checks
- backend production build
- frontend production build
- launch QA tests
- any available unit/integration tests

If no tests exist for a critical area, add lightweight tests for:
- unpaid report blocked
- unpaid PDF blocked
- paid entitlement unlocks report
- accessToken required
- Stripe webhook entitlement update
- preview returns ranges only

PART 9 — Manual test script
Create a clear manual QA checklist I can follow:
1. Create project.
2. Confirm free preview.
3. Confirm full report locked.
4. Click unlock.
5. Pay with Stripe test card.
6. Confirm success page.
7. Confirm full report unlocked.
8. Confirm PDF downloads.
9. Confirm another project cannot be accessed without token.
10. Confirm admin page requires ADMIN_TOKEN.

PART 10 — Final output
Give me a final report with:
1. What was broken.
2. What was fixed.
3. Current branch.
4. Commit SHA.
5. Frontend status.
6. Backend status.
7. Stripe checkout status.
8. Webhook status.
9. Paywall status.
10. PDF unlock status.
11. Database/migration status.
12. Geocoding status.
13. Tests passed.
14. Remaining blockers.
15. Exact manual steps I still need to take.

Do not stop after finding bugs. Fix them and rerun tests until the app is stable, unless the blocker requires my manual Stripe dashboard, Replit secrets, DNS, or GitHub approval action.